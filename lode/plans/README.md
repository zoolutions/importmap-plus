# Plans

Plans live in `../../docs/plans/` (written by `/plan`, executed by `/lfg`) and in GitHub issues on zoolutions/importmap-plus. This directory exists so the lode's shape matches Lode Coding's; nothing is duplicated here. Session handovers go in `../tmp/` and are not committed.
